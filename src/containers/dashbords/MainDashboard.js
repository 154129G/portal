import { Grid, Typography } from "@material-ui/core";

const MainDashboard = () =>{
    return(
        <Grid>
            <Typography>MAIN DASHBORD</Typography>
        </Grid>
    );
}

export default MainDashboard;